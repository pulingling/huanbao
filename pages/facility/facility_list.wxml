<view>
    <block wx:for="{{list}}" wx:for-item="item" wx:key="item.id">
        <facility-card  data-id="{{item.id}}" bindreloadData="reloadData" bindtap="toDetail" item="{{item}}" title="{{title}}"/>
    </block>
</view>